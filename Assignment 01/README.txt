marbles will be represented by numbers according to color.
user prompt to add new marbles. marbles to be added to the array.
user prompt to remove marbles. pick random marble and pop from array.

start with empty bag (array). add marbles (amount, random numbers for colors).
pop a random marble out of array and verify what number/color it is.
add marbles by typing specific command.
